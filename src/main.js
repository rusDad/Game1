window.addEventListener('load',()=>{const canvas=document.getElementById('game-canvas');window.Assets.load().then(()=>{const game=window.Game.create(canvas);let last=performance.now();function frame(now){const dt=Math.min(0.033,(now-last)/1000);last=now;window.Game.update(game,dt,now);window.Renderer.render(game);window.UI.render(game);requestAnimationFrame(frame);}requestAnimationFrame(frame);}).catch((e)=>{console.error('Asset load failure',e);});});
