window.Collision=(function(){function isWallTile(level,tx,ty){if(tx<0||ty<0||tx>=level.width||ty>=level.height)return true;return level.tiles[ty][tx]==='W';}function isBlockedPixelRect(level,x,y,w,h){const ts=window.CONFIG.TILE_SIZE;const x0=Math.floor(x/ts),y0=Math.floor(y/ts),x1=Math.floor((x+w-1)/ts),y1=Math.floor((y+h-1)/ts);for(let ty=y0;ty<=y1;ty++)for(let tx=x0;tx<=x1;tx++)if(isWallTile(level,tx,ty))return true;return false;}return{isWallTile,isBlockedPixelRect};})();
